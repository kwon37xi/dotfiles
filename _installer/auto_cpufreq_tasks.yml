---
# GNOME 자체의 성능 설정을 사용하기 때문에 잘 안쓰게 됨.
- name: install auto-cpufreq # https://github.com/AdnanHodzic/auto-cpufreq
  become: yes
  community.general.snap:
    channel: stable
    name: auto-cpufreq
- name: auto-cpufreq daemon
  become: yes
  shell: |
    auto-cpufreq --install
